<Window x:Class="yShorts.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Icon="favicon.ico"
        Width="560" Height="580"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0D0D0D">

    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#8B5CF6"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#1A1A2E"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#2A2A4A"/>
        <SolidColorBrush x:Key="TextBrush" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextDimBrush" Color="#94A3B8"/>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="SettingsTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Cascadia Mono, Consolas, monospace"/>
            <Setter Property="CaretBrush" Value="#E2E8F0"/>
            <Setter Property="ContextMenu">
                <Setter.Value>
                    <ContextMenu>
                        <MenuItem Command="Cut"/>
                        <MenuItem Command="Copy"/>
                        <MenuItem Command="Paste"/>
                    </ContextMenu>
                </Setter.Value>
            </Setter>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SaveButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="32,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#A78BFA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#CBD5E1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="32,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#334155"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="⚙ Settings"
                       FontSize="22" FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}"/>
            <TextBlock Text="Configure your API keys and tool paths"
                       FontSize="12" Foreground="{StaticResource TextDimBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Settings Fields -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- API Keys Section -->
                <TextBlock Text="API KEYS" FontSize="11" FontWeight="Bold"
                           Foreground="{StaticResource AccentBrush}" Margin="0,0,0,12"/>

                <TextBlock Text="Gemini API Key" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="GeminiKeyBox" Style="{StaticResource SettingsTextBox}"/>

                <TextBlock Text="Pexels API Key" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="PexelsKeyBox" Style="{StaticResource SettingsTextBox}"/>

                <!-- Separator -->
                <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,4,0,18"/>

                <!-- Tool Paths Section -->
                <TextBlock Text="TOOL PATHS" FontSize="11" FontWeight="Bold"
                           Foreground="{StaticResource AccentBrush}" Margin="0,0,0,12"/>

                <TextBlock Text="FFmpeg Path" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="FfmpegPathBox" Style="{StaticResource SettingsTextBox}"/>

                <TextBlock Text="FFprobe Path" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="FfprobePathBox" Style="{StaticResource SettingsTextBox}"/>

                <TextBlock Text="Edge-TTS Path" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="EdgeTtsPathBox" Style="{StaticResource SettingsTextBox}"/>

                <!-- Separator -->
                <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,4,0,18"/>

                <!-- Output Section -->
                <TextBlock Text="OUTPUT" FontSize="11" FontWeight="Bold"
                           Foreground="{StaticResource AccentBrush}" Margin="0,0,0,12"/>

                <TextBlock Text="Output Directory" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="OutputDirBox" Style="{StaticResource SettingsTextBox}"/>

                <TextBlock Text="Video Language" Style="{StaticResource FieldLabel}"/>
                <ComboBox x:Name="LanguageBox"
                          Margin="0,0,0,14" Padding="10,8"
                          Background="#111827" Foreground="{StaticResource TextBrush}"
                          BorderBrush="{StaticResource BorderBrush}"
                          FontSize="13" SelectionChanged="LanguageBox_SelectionChanged">
                    <ComboBox.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#111827"/>
                    </ComboBox.Resources>
                    <ComboBoxItem Content="English" IsSelected="True"/>
                    <ComboBoxItem Content="Hebrew"/>
                </ComboBox>

                <TextBlock Text="TTS Voice" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="TtsVoiceBox" Style="{StaticResource SettingsTextBox}"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource CancelButton}"
                    Click="Cancel_Click"
                    Margin="0,0,10,0"/>
            <Button Content="💾 Save"
                    Style="{StaticResource SaveButton}"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
